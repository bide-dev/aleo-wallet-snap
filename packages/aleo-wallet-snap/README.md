# `aleo-wallet-snap`

## Installation

```bash
yarn install
npx husky install
```

## Usage

Build deps:

```bash
cd ../aleo-wasm-bundler
yarn install --check-files
yarn build:clean
```

Now build this project:

```bash
yarn install --check-files
yarn build
yarn serve
```
